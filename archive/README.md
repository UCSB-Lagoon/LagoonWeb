# Archive

Things kept for history but no longer active.

- **`scripts/`** — one-off Python that injected the "Related guides" block and
  "Updated:" date stamp into the marketing guide pages. **Stale since the
  2026-05-15 site merge**: they target old repo-root `<slug>/index.html`
  paths that no longer exist (pages now live in `web/public/marketing/`).
  Their output is already baked into the committed HTML. If you need to
  re-run that kind of transform, rewrite against `web/public/marketing/*.html`.
- **`screenshots/legacy/`** — early Figma exports and iPhone screenshots used
  during the first design pass. Not referenced by the app.
